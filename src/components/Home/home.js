import React from 'react';
import Meetups from '../../containers/meetup_container';

const Home = () => {
    return (
        <div>
            <Meetups/>
        </div>
    );
};

export default Home;